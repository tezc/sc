name: windows
on:
  schedule:
    - cron: '0 0 * * 0'
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  windows:
    runs-on: windows-2025
    name: Build on Windows

    steps:
      - uses: actions/checkout@v4
      - name: build
        run: |
          mkdir build-debug && cd build-debug
          cmake -A x64 ..
          cmake --build .
          ctest -C Debug